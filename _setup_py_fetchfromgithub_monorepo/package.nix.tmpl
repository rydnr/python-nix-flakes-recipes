# Generated by the package.nix.tmpl template in the setup.py+fetchfromgithub+monorepo recipe for [https://github.com/rydnr/python-nix-flake-generator]. Changes will be overwritten.
{{ buildPythonPackage, fetchFromGitHub, lib{dependencies_declaration} }}:
buildPythonPackage rec {{
  pname = "{package_name}";
  version = "{package_version}";
  format = "setuptools";

  src = fetchFromGitHub {{
    owner = "{repo_owner}";
    repo = "{repo_name}";
    rev = "{repo_rev}";
    sha256 = "{repo_sha256}";
  }};

  nativeBuildInputs = [
    {native_build_inputs_names_with_newlines}
  ];

  propagatedBuildInputs = [
    {propagated_build_inputs_names_with_newlines}
  ];

  buildInputs = [
    {build_inputs_names_with_newlines}
  ];

  checkInputs = [
    {check_inputs_names_with_newlines}
  ];

  meta = with lib; {{
    description = ''
{package_description}
'';
    license = licenses.{package_license};
    homepage = "{repo_url}";
    maintainers = with maintainers; [ ];
    platforms = platforms.all;
  }};
}}
